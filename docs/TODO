GC
- native method need to preserve the stack
- main thread should have reference to its stack

Bytecode verification

Remove Reflections dependency

Proper Heap options

is RegisterNatives a special case for native methods?

JMX?

Reflection?

Volatile (use UNSAFE)

Releasing locks on throwing!

Runtime exceptions
- NPE
- DivideBy0
- ArrayIndex
- ClassCast

Bugs:
- System.out.println newline often missing
- apparently synchronized METHODS don't create the monitor enter/exit opcodes, must be done manually (as opposed to blocks, which do).